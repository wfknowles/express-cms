<!-- dashboard -->
<div class="container">
    <div class="row">
        <div class="col-md-12 posts-wrapper">
            {{#eachWith posts data=this}}
                {{> post }}
            {{/eachWith}}
        </div>
    </div>
</div>